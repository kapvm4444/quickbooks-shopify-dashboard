# Start and Connect

Backend

```cmd
cd quickbooks-server
npm install
npm start
```

Frontend

```cmd
cd shopify-dashboard
npm install
npm run dev
```

Connect

- Open the frontend URL shown by the Vite server.
- Go to the Expenses tab and click the Connect button to start the QuickBooks OAuth flow.
